/**
 * @fileoverview Barrel export for all sports-mcp-server resource definitions.
 * @module mcp-server/resources/definitions/index
 */

// No resources — sports data is live and time-sensitive; tool surface covers all access patterns.
export const allResourceDefinitions: never[] = [];
