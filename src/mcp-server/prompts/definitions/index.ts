/**
 * @fileoverview Barrel export for all sports-mcp-server prompt definitions.
 * @module mcp-server/prompts/definitions/index
 */

// No prompts — this is a pure data server with no recurring interaction templates.
export const allPromptDefinitions: never[] = [];
